# Unsupported Release Target

Linux GUI support has been removed from the release matrix. Use the shared CLI on Linux:

```bash
veil-node --help
veil-node doctor
veil-node seal ...
veil-node open ...
```

This folder is retained only as an explicit unsupported-target notice. Linux GUI code is not packaged in new releases.
